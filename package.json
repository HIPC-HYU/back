{
  "name": "hipc_backend",
  "version": "1.0.0",
  "description": "server for HYU IS algorithm page",
  "main": "dist/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/app.js",
    "build": "npx tsc",
    "dev": "concurrently \"tsc --watch\" \"nodemon -q dist/app.js\"",
    "dev:ts": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/app.ts",
    "watch": "tsc -w"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
